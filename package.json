{
  "name": "curriculum-miguel",
  "version": "0.1.0",
  "private": true,
  "description": "Personal curriculum site — AWS CDK infrastructure",
  "bin": {
    "curriculum-miguel": "bin/curriculum-miguel.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth -c env=dev",
    "synth:prod": "cdk synth -c env=prod",
    "diff": "cdk diff -c env=dev",
    "diff:prod": "cdk diff -c env=prod",
    "deploy": "cdk deploy -c env=dev --require-approval never",
    "deploy:prod": "cdk deploy -c env=prod",
    "destroy": "cdk destroy -c env=dev",
    "destroy:prod": "cdk destroy -c env=prod",
    "bootstrap": "cdk bootstrap"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "^2.1000.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.7.2"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.178.0",
    "constructs": "^10.4.2"
  }
}
